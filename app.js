const grid=document.querySelector('#projectsGrid');let projects=[];const modal=document.querySelector('#galleryModal'),gallery=document.querySelector('#gallery');fetch('projects.json').then(r=>{if(!r.ok)throw new Error('projects.json');return r.json()}).then(data=>{projects=data;render('all')}).catch(()=>{grid.innerHTML='<p>بارگذاری پروژه‌ها با خطا مواجه شد.</p>'});function render(filter){grid.innerHTML='';projects.filter(p=>filter==='all'||p.service===filter).forEach(p=>{const card=document.createElement('article');card.className='project-card';card.tabIndex=0;card.innerHTML=`<img loading="lazy" src="${p.cover}" alt="${p.title}"><div class="project-overlay"><span>${p.service}</span><h3>${p.title}</h3><p>${p.meta.join(' • ')} • ${p.images.length} تصویر منتخب</p></div>`;card.onclick=()=>openGallery(p);card.onkeydown=e=>{if(e.key==='Enter'||e.key===' ')openGallery(p)};grid.append(card)})}document.querySelectorAll('.filter-bar button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter-bar button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)});function openGallery(p){document.querySelector('#modalTitle').textContent=p.title;document.querySelector('#modalService').textContent=p.service;document.querySelector('#modalSummary').textContent=p.summary;document.querySelector('#modalMeta').innerHTML=p.meta.map(x=>`<b>${x}</b>`).join('');gallery.innerHTML='';p.images.forEach((src,i)=>gallery.insertAdjacentHTML('beforeend',`<a href="${src}" target="_blank" rel="noopener"><img loading="lazy" src="${src}" alt="${p.title} - تصویر ${i+1}"></a>`));modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''}document.querySelector('.close').onclick=closeModal;modal.onclick=e=>{if(e.target===modal)closeModal()};document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});const menu=document.querySelector('.menu-btn'),nav=document.querySelector('nav');menu.onclick=()=>nav.classList.toggle('open');nav.querySelectorAll('a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));